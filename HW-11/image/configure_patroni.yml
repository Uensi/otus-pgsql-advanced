---
- name: Configure and start Patroni
  hosts: ha_cluster
  become: true
  # Ноды стартуют по очереди: первая инициализирует кластер (Leader), остальные клонируются и становятся Replica.
  serial: 1

  tasks:
    - name: Render Patroni config
      ansible.builtin.template:
        src: templates/patroni.yml.j2
        dest: "{{ patroni_config_dir }}/patroni.yml"
        owner: postgres
        group: postgres
        mode: "0640"

    - name: Install Patroni systemd unit
      ansible.builtin.copy:
        dest: /etc/systemd/system/patroni.service
        mode: "0644"
        content: |
          [Unit]
          Description=Patroni - PostgreSQL HA orchestrator
          After=network.target etcd.service
          Wants=etcd.service

          [Service]
          Type=simple
          User=postgres
          Group=postgres
          ExecStart=/usr/local/bin/patroni {{ patroni_config_dir }}/patroni.yml
          Restart=always
          RestartSec=5
          TimeoutSec=300

          [Install]
          WantedBy=multi-user.target

    - name: Reload systemd and start Patroni
      ansible.builtin.systemd:
        name: patroni
        state: started
        enabled: true
        daemon_reload: true

    - name: Wait for Patroni REST API to answer
      ansible.builtin.uri:
        url: "http://{{ private_ip }}:{{ patroni_rest_port }}/patroni"
        status_code: 200
      register: patroni_api
      until: patroni_api.status == 200
      retries: 24
      delay: 5
